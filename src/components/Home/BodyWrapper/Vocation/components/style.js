import styled from 'styled-components';
import { Row } from 'antd';

export const ComRow = styled(Row)`
  .ant-col{
    text-align: center;
    padding: 10px 0;
  }
  .selected{
    background: #00CCCC;
  }
`;